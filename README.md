# golang-custom-deepequal
A re-implementation of reflect.DeepEqua that allows registering of custom deep equal function for specified types. The custom deep equals work recursively when comparing structs
